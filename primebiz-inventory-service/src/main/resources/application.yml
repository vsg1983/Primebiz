server:
  port: 8083

spring:
  application:
    name: primebiz-inventory-service
  datasource:
    url: jdbc:postgresql://localhost:5432/primebiz_inventory
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
