server:
  port: 8088

spring:
  application:
    name: primebiz-notification-service
  datasource:
    url: jdbc:postgresql://localhost:5432/primebiz_notification
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      uri: http://localhost:8888

spring:
  cloud:
    stream:
      bindings:
        paymentCompleted-in-0:
          destination: payment-events
          group: notification-service-group


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
