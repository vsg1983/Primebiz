server:
  port: 8082

spring:
  application:
    name: primebiz-product-service
  datasource:
    url: jdbc:postgresql://localhost:5432/primebiz_product
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
